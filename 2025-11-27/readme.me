# 📈 LeetCode 3381 — Maximum Subarray Sum With Length Divisible by K

**Difficulty:** Medium  
**Tags:** Prefix Sum, Modulo, DP, Subarray

---

## 📝 Summary
Find the **maximum subarray sum** where the **length is divisible by k**.

---

## 💡 Intuition
A subarray `nums[l..r]` satisfies:

- `sum = prefix[r] - prefix[l-1]`
- length divisible by k → `(r + 1) % k == l % k`

So we track the **minimum prefix sum** for each modulo class.

---

## 🧠 Approach
For each index:

- `prefix += nums[i]`
- `r = (i + 1) % k`

Valid subarray formula:

```
max sum = prefix - best[r]
```

Where `best[r]` stores the **smallest prefix** with that remainder.

---

## 🕒 Complexity
- **Time:** O(n)  
- **Space:** O(k)

---

## ✅ C++ Solution

```cpp
class Solution {
public:
    long long maxSubarraySum(vector<int>& nums, int k) {
        int n = nums.size();
        vector<long long> best(k, LLONG_MAX);

        long long prefix = 0, ans = LLONG_MIN;
        best[0] = 0;  // prefix before starting

        for(int i = 0; i < n; i++){
            prefix += nums[i];
            int r = (i + 1) % k;

            if(best[r] != LLONG_MAX)
                ans = max(ans, prefix - best[r]);

            best[r] = min(best[r], prefix);
        }

        return ans;
    }
};
```

---

## 📌 Notes
- Uses **prefix-sum modulo class grouping**
- Tracks the best (minimum) prefix for each modulo
- Ensures subarray length divisible by **k**
